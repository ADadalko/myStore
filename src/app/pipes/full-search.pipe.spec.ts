import { FullSearchPipe } from './full-search.pipe';

describe('FullSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FullSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
